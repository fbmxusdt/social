# Session: 2026-07-06 — HALVING TIME hero + cPanel 404 root-cause fix

## Summary
Built the mobile-first HALVING TIME announcement hero on the landing page, then root-caused
and fixed why fresh builds 404'd on fbmx.app (cPanel/LiteSpeed): the SMB workspace volume
forces mode 700 on build output, and zip→Extract preserves it, making files unreadable to
the web server.

## What Was Done
- New `HalvingHero.jsx`: milestone badge, gold-gradient H1, animated 70%-locked liquidity
  meter, benefit chips, PancakeSwap buy CTA (new tab), and "−50% Passive Rewards / Scarcity
  by Design" card. Copy rewritten from the user's draft to a credible tokenomics framing.
- Wired hero into `Connect.jsx` above the connect/register gate (wrong-network stays bare).
- Fixed missing `xs: 400px` breakpoint in `tailwind.config.js` — `xs:` classes in
  RegisterPanel/CollectWithdrawPanels/Activate were silently inert until now.
- Added `public/.htaccess` (Apache/LiteSpeed SPA fallback → index.html); GH Pages ignores it.
- Root-caused fbmx.app all-URLs-404: dist files are `-rwx------` (700) on the SMB volume;
  macOS zip stores that; cPanel Extract preserves it; LiteSpeed reports unreadable files
  as 404. Confirmed by user in cPanel File Manager (extracted files 0700).
- Packaging generalized workspace-wide: shared `/Volumes/ClaudeOs/tools/package-cpanel.sh`
  + guarded npm `postbuild` hook, so plain `npm run build` auto-creates `dist.zip`
  (dirs 755, files 644). Wired into fbmxsocial, fbmxdao, aeos-system/app (all verified);
  documented in `core-technology-template.md` §5; per-project script removed.
- Verified hero on 390px/430px viewports via headless browser; clean production builds.

## Current State
- Code done, build green. `dist.zip` generated and permission-verified.
- Waiting on user to upload the zip to the fbmx.app document root and Extract, then
  probe `https://fbmx.app/index.html`, `/`, `/register` (expect 200/200/200) and confirm
  the new asset hash (`index-BvnMAUCD.js`) replaces the backup's (`index-BVWO6s74.js`).
- Rescue path for a broken upload without re-uploading: cPanel Terminal in the docroot:
  `find . -type d -exec chmod 755 {} +` and `find . -type f -exec chmod 644 {} +`.

## Files Modified
- `src/components/HalvingHero.jsx` — new
- `src/pages/Connect.jsx` — hero + #get-started section; full-width mobile CTA
- `tailwind.config.js:7-9` — added `screens.xs = 400px`
- `public/.htaccess` — new (SPA fallback)
- `package.json` — added guarded `postbuild` hook → shared `tools/package-cpanel.sh`
- `.gitignore` — ignore `dist.zip`

## Known Issues Carried Forward
- `Connect.jsx` "Enter App" still links to `/dashboard` (route doesn't exist → bounces
  to `/`). Decide target (`/activate`?) next session.
- `public/favicon.svg` missing — index.html references it; harmless 404 on every host.

## Next Phase
- User uploads zip → live probes from here → confirm HALVING TIME hero live on fbmx.app.
- Then: commit session, decide `/dashboard` link target, add favicon.

## Time Spent
~2.5 h
